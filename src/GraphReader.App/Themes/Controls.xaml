<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright 2026 Sungwoo Kang -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{DynamicResource App.FontFamily.Default}" />
    <Setter Property="FontSize" Value="{DynamicResource App.FontSize.Body}" />
    <Setter Property="LineHeight" Value="{DynamicResource App.LineHeight.Body}" />
    <Setter Property="LineStackingStrategy" Value="BlockLineHeight" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Style.Triggers>
      <Trigger Property="FontSize" Value="12">
        <Setter Property="LineHeight" Value="{DynamicResource App.LineHeight.Caption}" />
      </Trigger>
      <Trigger Property="FontSize" Value="18">
        <Setter Property="LineHeight" Value="{DynamicResource App.LineHeight.Subtitle}" />
      </Trigger>
      <Trigger Property="FontSize" Value="24">
        <Setter Property="LineHeight" Value="{DynamicResource App.LineHeight.Title}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="App.Style.FocusVisual" TargetType="Control">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate>
          <Border Margin="-3"
                  BorderBrush="{DynamicResource App.Brush.Focus}"
                  BorderThickness="{DynamicResource App.Thickness.Focus}"
                  CornerRadius="{DynamicResource App.CornerRadius.Medium}">
            <AdornedElementPlaceholder />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="App.Style.Button" TargetType="Button">
    <Setter Property="MinHeight" Value="{DynamicResource App.Control.Height}" />
    <Setter Property="Padding" Value="{DynamicResource App.Thickness.ControlPadding}" />
    <Setter Property="Background" Value="{DynamicResource App.Brush.Surface}" />
    <Setter Property="BorderBrush" Value="{DynamicResource App.Brush.Border}" />
    <Setter Property="BorderThickness" Value="{DynamicResource App.Control.BorderWidth}" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="FontFamily" Value="{DynamicResource App.FontFamily.Default}" />
    <Setter Property="FontSize" Value="{DynamicResource App.FontSize.Body}" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource App.Style.FocusVisual}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Chrome"
                  Padding="{TemplateBinding Padding}"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{DynamicResource App.CornerRadius.Medium}"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              RecognizesAccessKey="True" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource App.Brush.Primary}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.8" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextDisabled}" />
              <Setter TargetName="Chrome" Property="Opacity" Value="0.62" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{DynamicResource App.Brush.PrimarySubtle}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="App.Style.PrimaryButton" BasedOn="{StaticResource App.Style.Button}" TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource App.Brush.Primary}" />
    <Setter Property="BorderBrush" Value="{DynamicResource App.Brush.Primary}" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextOnPrimary}" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{DynamicResource App.Brush.PrimaryHover}" />
        <Setter Property="BorderBrush" Value="{DynamicResource App.Brush.PrimaryHover}" />
      </Trigger>
      <Trigger Property="IsPressed" Value="True">
        <Setter Property="Background" Value="{DynamicResource App.Brush.PrimaryPressed}" />
        <Setter Property="BorderBrush" Value="{DynamicResource App.Brush.PrimaryPressed}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="App.Style.SubtleButton" BasedOn="{StaticResource App.Style.Button}" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
  </Style>

  <Style x:Key="App.Style.IconButton" BasedOn="{StaticResource App.Style.SubtleButton}" TargetType="Button">
    <Setter Property="Width" Value="36" />
    <Setter Property="Padding" Value="8" />
  </Style>

  <Style x:Key="App.Style.ToggleButton" TargetType="ToggleButton">
    <Setter Property="MinHeight" Value="{DynamicResource App.Control.Height}" />
    <Setter Property="Padding" Value="{DynamicResource App.Thickness.ControlPadding}" />
    <Setter Property="Background" Value="{DynamicResource App.Brush.Surface}" />
    <Setter Property="BorderBrush" Value="{DynamicResource App.Brush.Border}" />
    <Setter Property="BorderThickness" Value="{DynamicResource App.Control.BorderWidth}" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="FontFamily" Value="{DynamicResource App.FontFamily.Default}" />
    <Setter Property="FontSize" Value="{DynamicResource App.FontSize.Body}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource App.Style.FocusVisual}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="Chrome"
                  Padding="{TemplateBinding Padding}"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{DynamicResource App.CornerRadius.Medium}">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              RecognizesAccessKey="True" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource App.Brush.Primary}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource App.Brush.PrimarySubtle}" />
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource App.Brush.Primary}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextDisabled}" />
              <Setter TargetName="Chrome" Property="Opacity" Value="0.62" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="App.Style.TextBox" TargetType="TextBox">
    <Setter Property="MinHeight" Value="{DynamicResource App.Control.Height}" />
    <Setter Property="Padding" Value="{DynamicResource App.Thickness.CompactPadding}" />
    <Setter Property="Background" Value="{DynamicResource App.Brush.Surface}" />
    <Setter Property="BorderBrush" Value="{DynamicResource App.Brush.Border}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="CaretBrush" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="FontFamily" Value="{DynamicResource App.FontFamily.Default}" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource App.Style.FocusVisual}" />
  </Style>

  <Style x:Key="App.Style.ComboBox" TargetType="ComboBox">
    <Setter Property="MinHeight" Value="{DynamicResource App.Control.Height}" />
    <Setter Property="Padding" Value="{DynamicResource App.Thickness.CompactPadding}" />
    <Setter Property="Background" Value="{DynamicResource App.Brush.Surface}" />
    <Setter Property="BorderBrush" Value="{DynamicResource App.Brush.Border}" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="FontFamily" Value="{DynamicResource App.FontFamily.Default}" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource App.Style.FocusVisual}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <ToggleButton Background="{TemplateBinding Background}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          ClickMode="Press"
                          Focusable="False"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
              <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                  <Border Background="{TemplateBinding Background}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{DynamicResource App.CornerRadius.Medium}">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="28" />
                      </Grid.ColumnDefinitions>
                      <ContentPresenter Margin="10,2,4,2"
                                        VerticalAlignment="Center"
                                        Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        ContentTemplateSelector="{Binding ItemTemplateSelector, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        TextElement.Foreground="{DynamicResource App.Brush.Text}" />
                      <Path Grid.Column="1"
                            Width="8"
                            Height="5"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Data="M0,0 L4,4 L8,0"
                            Stroke="{DynamicResource App.Brush.Text}"
                            StrokeThickness="1.5" />
                    </Grid>
                  </Border>
                </ControlTemplate>
              </ToggleButton.Template>
            </ToggleButton>
            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   Placement="Bottom"
                   PopupAnimation="None">
              <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                      MaxHeight="280"
                      Margin="0,4,0,0"
                      Background="{DynamicResource App.Brush.Surface}"
                      BorderBrush="{DynamicResource App.Brush.Border}"
                      BorderThickness="1"
                      CornerRadius="{DynamicResource App.CornerRadius.Medium}">
                <ScrollViewer>
                  <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                </ScrollViewer>
              </Border>
            </Popup>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="App.Style.CheckBox" TargetType="CheckBox">
    <Setter Property="MinHeight" Value="28" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="FontFamily" Value="{DynamicResource App.FontFamily.Default}" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource App.Style.FocusVisual}" />
  </Style>

  <Style x:Key="App.Style.ListBoxItem" TargetType="ListBoxItem">
    <Setter Property="Padding" Value="8,6" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Tag" Value="{DynamicResource App.Motion.Enabled}" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource App.Style.FocusVisual}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <Border x:Name="Chrome"
                  Padding="{TemplateBinding Padding}"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{DynamicResource App.CornerRadius.Medium}"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource App.Brush.PrimarySubtle}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter Property="Foreground" Value="{DynamicResource App.Brush.SelectionText}" />
              <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource App.Brush.Selection}" />
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource App.Brush.Primary}" />
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsSelected" Value="True" />
                <Condition Property="Tag" Value="True" />
              </MultiTrigger.Conditions>
              <MultiTrigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="Chrome"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0.88"
                                     To="1"
                                     Duration="0:0:0.14" />
                  </Storyboard>
                </BeginStoryboard>
              </MultiTrigger.EnterActions>
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextDisabled}" />
              <Setter TargetName="Chrome" Property="Opacity" Value="0.62" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="App.Style.ListItemButton" BasedOn="{StaticResource App.Style.SubtleButton}" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="App.Style.TabItem" TargetType="TabItem">
    <Setter Property="MinHeight" Value="36" />
    <Setter Property="Padding" Value="12,6" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="FontFamily" Value="{DynamicResource App.FontFamily.Default}" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource App.Style.FocusVisual}" />
  </Style>

  <Style x:Key="App.Style.Panel" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource App.Brush.Surface}" />
    <Setter Property="BorderBrush" Value="{DynamicResource App.Brush.Border}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{DynamicResource App.CornerRadius.Large}" />
  </Style>

  <Style x:Key="App.Style.Card" BasedOn="{StaticResource App.Style.Panel}" TargetType="Border">
    <Setter Property="Padding" Value="12" />
    <Setter Property="Background" Value="{DynamicResource App.Brush.SurfaceRaised}" />
  </Style>

  <Style TargetType="ComboBoxItem">
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="Background" Value="{DynamicResource App.Brush.Surface}" />
    <Setter Property="Foreground" Value="{DynamicResource App.Brush.Text}" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource App.Style.FocusVisual}" />
    <Style.Triggers>
      <Trigger Property="IsHighlighted" Value="True">
        <Setter Property="Background" Value="{DynamicResource App.Brush.Selection}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="TextBox" BasedOn="{StaticResource App.Style.TextBox}" />
  <Style TargetType="ComboBox" BasedOn="{StaticResource App.Style.ComboBox}" />
  <Style TargetType="CheckBox" BasedOn="{StaticResource App.Style.CheckBox}" />
  <Style TargetType="ListBoxItem" BasedOn="{StaticResource App.Style.ListBoxItem}" />
  <Style TargetType="TabItem" BasedOn="{StaticResource App.Style.TabItem}" />
</ResourceDictionary>
