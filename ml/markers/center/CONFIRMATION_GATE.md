# Marker-center confirmation gate

This split was frozen after the three validation-only candidates were trained
and P1 was selected. It does not reuse any scene from the failed first public
gate. The same exact-count, duplicate, and prohibited-structure gates apply.

The historical P1 confirmation is unsealed failed evidence and is additionally
invalidated by the training-protocol deviation documented in
`PRODUCTION_TRAINING_GATE.md`. It cannot be replayed or promoted.
