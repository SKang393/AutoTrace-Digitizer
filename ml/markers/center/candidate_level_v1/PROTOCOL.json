{
  "schema": "graphreader.marker-center-candidate-protocol.v1",
  "task": "marker-center",
  "revision": "marker-center-candidate-level-v1",
  "defect_class": "candidate-level marker proposal classification and center regression",
  "prior_revision": "marker-center-production-repair-v2",
  "prior_revision_reuse": false,
  "experiment_budget": 3,
  "currently_preregistered_candidate": null,
  "selected_candidate": "P3",
  "sealed_public_gate_authorized": false,
  "architecture": "candidate-spatial-patch-cnn-v2-export-safe",
  "consumed_candidates": {
    "P1": "failed frozen validation with 0 of 9 exact scenes, 134 false positives, and 7 duplicates at threshold 0.7; then failed ONNX export at unsupported adaptive 5-to-3 pooling",
    "P2": "passed CPU ONNX parity and removed all false positives, duplicates, and prohibited hits, but missed 1 of 63 validation markers at threshold 0.7"
  },
  "p2_isolated_change": "fixed export-safe spatial pool plus one deterministic train-split hard-negative refinement phase with unweighted classification loss",
  "p3_isolated_change": "reuse the exact P2 checkpoint and ONNX with zero optimizer steps and freeze the single validation-selected threshold 0.2",
  "p3_selection_result": "9 of 9 exact validation scenes, zero false positives, false negatives, duplicates, and prohibited hits; CPU ONNX parity maximum absolute error 1.1920928955078125e-6",
  "sealed_public_gate_result": "failed 11 of 16 exact scenes with one false positive, two false negatives, zero duplicates, and two divider hits; the once-only evaluation and all three candidate slots are consumed",
  "input_contract": {
    "name": "candidate_patches",
    "shape": ["candidate_count", 3, 33, 33],
    "channels": ["ink_probability", "text_mask", "artifact_mask"]
  },
  "output_contract": {
    "name": "candidate_predictions",
    "shape": ["candidate_count", 4],
    "columns": ["marker_probability", "offset_x_grid", "offset_y_grid", "radius_pixels"]
  },
  "proposal_stride_pixels": 4,
  "selection_thresholds": [0.2],
  "sealed_gate_requirements": {
    "exact_count_every_scene": true,
    "false_positives": 0,
    "false_negatives": 0,
    "duplicates": 0,
    "prohibited_structure_hits": 0,
    "onnx_parity_maximum_absolute_error": 0.00001,
    "provider": "CPUExecutionProvider"
  },
  "prohibited_structures": [
    "text",
    "axis",
    "tick",
    "divider",
    "bracket",
    "arrowhead",
    "legend",
    "line_intersection"
  ],
  "private_or_article_images": false,
  "chandler_included": false,
  "public_contract_schema_changes": false,
  "production_approval": false,
  "release_eligible": false
}
