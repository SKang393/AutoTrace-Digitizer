# OCR composite proposal and role V11

This defect class is preregistered from aggregate, already-consumed V2 composition evidence. It may not read the V2 fixture archive, V2 scene truths, Chandler, private images, or article images.

P1 is a new Apache-2.0 multi-head model. It keeps the production proposal grouping algorithm and extends the existing visual and geometry tensor with normalized global proposal position, producing `[N,2,32,144]`. Its `[N,10]` output contains two proposal logits followed by eight role logits in the frozen order `YTick`, `XTick`, `AxisTitle`, `PhaseHeading`, `LegendText`, `Participant`, `Annotation`, `Other`.

The split was generated once after preregistration. Its tracked manifests bind 320 training scenes, 96 validation scenes, 128 hidden-public scenes, exact proposal fingerprints, candidate configurations, and runner and evaluator source bundles. P1 is consumed: it passed every fixed validation scene, region, role, and exclusion metric, but its CPU ONNX parity error was `1.0326504707336426e-05`, just above the fixed `1e-05` maximum. P2 is consumed and selected: its zero-optimizer exact-P1-checkpoint export scales all logits by `0.5`, preserves every argmax and probability ordering, passes 96 of 96 validation scenes and all 768 regions and roles exactly, and reduces parity error to `4.76837158203125e-06`. Its only hidden-public run then passed all 128 scenes and 1,024 regions and roles exactly through 128 direct CPU calls, with zero false regions, misses, duplicates, or prohibited-structure hits. Public report SHA-256 is `9259765bcb188804ba78a427699ad77910b41c17605515ed799d5cb9feb52840`. P3 remains unregistered. P1, P2, and the public gate are consumed and may not rerun. No passing candidate is production-approved until a separate checksum-bound C# composition gate also proves recognition, roles, markers, provenance, CPU execution, model-store discovery, notices, and packaging.
