# OCR morphology proposal and role V13

V13 is a new defect class opened only from aggregate evidence produced by the consumed V12 hidden-public run. That run found 155 of 160 scenes exact, with four false regions, one missed region, zero duplicates, and perfect accepted-region role classification. No V12 hidden-public fixture bytes, truth, case identity, image, or case-level failure detail was inspected or reused, and the V12 gate cannot rerun.

The preregistered P1 change is a fresh renderer spanning text scale, stroke, grid, connector, marker, and dropout morphology and a new dual-context network with parallel horizontal and vertical anisotropic branches. The production proposal grouping and `[N,2,32,144]` input to `[N,10]` output contract remain unchanged. The candidate budget is three. Selection and hidden-public gates require zero false, missed, duplicate, and prohibited-structure regions in every scene, role accuracy of at least `0.90`, every role at least `0.85`, and CPU ONNX maximum absolute parity error at most `1e-5`.

The split was materialized once from the committed generator. The tracked manifests bind 600 training scenes, 160 validation scenes, and 224 hidden-public scenes. The training tensor contains 50,715 proposals, including all 4,800 text truths and 45,915 morphology-family negatives. The hidden-public fixture archive SHA-256 is `f742e367aed9a96a0bba433cbbe218c4d61889c1ec56fc8b97987c8062466957`; its truth remains unavailable to the candidate runner and its evaluation limit remains one.

P1 is consumed and failed selection. Its direct CPU run found all 1,280 validation text regions with all eight roles exact, but retained one prohibited structural proposal in every fixed threshold comparison, producing 159 of 160 exact scenes. CPU ONNX parity also failed at `1.52587890625e-05` against the fixed `1e-5` maximum. The checksum-bound result is `P1_RESULT.json`; its report SHA-256 is `abc39cdb055c84e301ddcb927b94eafb0ac412d79abaa03a42f1cda81f31d52a` and its ONNX SHA-256 is `1da78b5c69ceca7dea3e125bf1af6a84a22278c293ec1c0d41eefa6c9432f8d6`.

P1 cannot rerun. P2 and P3 remain unregistered and cannot execute until an isolated candidate change is preregistered and committed against this aggregate result. The hidden-public gate remains unopened with zero evaluations. Production approval, model-store promotion, packaging, private Chandler validation, and release eligibility remain false.
